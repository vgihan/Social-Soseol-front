import FlexCenter from '../../../utils/FlexCenter';
import Logo from '../../../utils/Logo';

const LogoArea = () => {
  return (
    <FlexCenter>
      <Logo />
    </FlexCenter>
  );
};

export default LogoArea;
